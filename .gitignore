# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.log

# Dependencies (not used by the static site, but just in case)
node_modules/

# Local env / secrets
.env
.env.local

# Editor
.vscode/
.idea/

# Local Claude Code tooling (preview config, local settings)
.claude/

# Source/originals & temp image-processing artifacts (not needed by the live site)
logo/
.tmp_thumbs/
*.tmp
